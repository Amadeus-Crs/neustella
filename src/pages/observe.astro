---
/**
 * 观察分类页
 * 展示所有观察类文章
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

const articles = await getCollection('articles', (entry) =>
  entry.data.category === 'observe' && !entry.data.draft
);
articles.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
---

<BaseLayout title="观察 — Neustella" description="观察与思考">
  <div class="max-w-2xl mx-auto px-6 py-12">
    <header class="mb-10">
      <div class="flex items-center gap-3 mb-2">
        <span class="w-2 h-2 rounded-full bg-[#c8a87c]"></span>
        <h1 class="text-3xl font-light text-[#f5f5f0] tracking-wide">观察</h1>
      </div>
      <p class="text-sm text-[#a0a098]">日常、阅读与生活的记录</p>
    </header>

    <div class="space-y-4">
      {articles.length === 0 && (
        <p class="text-sm text-[#a0a098] text-center py-12">暂无观察类文章</p>
      )}
      {articles.map((article) => (
        <ArticleCard
          title={article.data.title}
          date={article.data.date}
          description={article.data.description}
          slug={article.id}
          category={article.data.category}
        />
      ))}
    </div>
  </div>
</BaseLayout>
